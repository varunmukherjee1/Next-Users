export type User = {
    name: string,
    id: number,
    email: string,
    image: string,
    dob: string,
}

export type LoginUser = {
    uid: string,
    email: string,
    name: string,
    image: string
}